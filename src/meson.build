# SPDX-License-Identifier: GPL-2.0-only

subdir('curl')
subdir('git')
subdir('pcre2')
subdir('sqlite')

subdir('cves') # needs git
subdir('helpers') # needs curl & git
subdir('kerncvs') # needs curl & git & pcre2
