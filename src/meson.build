# SPDX-License-Identifier: GPL-2.0-only

subdir('curl')
subdir('helpers')
subdir('pcre2')
subdir('sqlite')

subdir('git') # needs helpers

subdir('cves') # needs git
subdir('kerncvs') # needs curl & git & pcre2
